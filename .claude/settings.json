{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); echo \"$f\" | grep -q 'engine/' && make build 2>/dev/null || true",
            "statusMessage": "Rebuilding Go engine...",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
